/**
 @param {array} valueToCheck - The array to check if it's an array.
 @returns {boolean} - Returns true is value is array.
 */

export default function isArray(valueToCheck) {
  return Array.isArray(valueToCheck)
}
